@model IEnumerable<Blood_Donation_Project.Models.BloodTypeTable>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BloodType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BloodType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BloodType_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BloodType_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BloodType_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
